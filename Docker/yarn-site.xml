<configuration>
    <!-- Dirección del ResourceManager -->
    <property>
        <name>yarn.resourcemanager.address</name>
        <value>localhost:8032</value>
    </property>

    <!-- Dirección del ResourceManager para el ResourceManager Web UI -->
    <property>
        <name>yarn.resourcemanager.scheduler.address</name>
        <value>localhost:8030</value>
    </property>

    <!-- Dirección de los nodos del ResourceManager -->
    <property>
        <name>yarn.resourcemanager.resource-tracker.address</name>
        <value>localhost:8031</value>
    </property>

    <!-- Directorio de trabajo de los nodos en YARN -->
    <property>
        <name>yarn.nodemanager.local-dirs</name>
        <value>/tmp/nm-local-dir</value>
    </property>

    <!-- Directorio de los registros de los nodos en YARN -->
    <property>
        <name>yarn.nodemanager.log-dirs</name>
        <value>/tmp/logs</value>
    </property>

    <!-- Dirección para acceder al Web UI de YARN -->
    <property>
        <name>yarn.resourcemanager.webapp.address</name>
        <value>localhost:8088</value>
    </property>
</configuration>